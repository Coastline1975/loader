
local a=game:GetService("Players")local a=a.LocalPlayer;local b=workspace:FindFirstChild("Lobby")local b={teleportCooldown=1.85}local c=os.clock()function getCharacter()local a=a.Character;if a then local b=a:FindFirstChild("HumanoidRootPart")if b then return a,b end end end;function collectCoin(a)repeat local b,c=getCharacter()if b and c then c.Position=a.Position+Vector3.new(0,-1+math.random()*2,0)end until not a:FindFirstChild("TouchInterest")end;function goToLobby()local a,b=getCharacter()if a and b then b.Position=Vector3.new(132.87,140.25,60.69)end end;function getCoinContainer()for a,a in ipairs(workspace:GetChildren())do local a=a:FindFirstChild("CoinContainer")if a then return a end end end;while true do local a=getCoinContainer()if os.clock()-c>=b.teleportCooldown and a then for a,a in ipairs(a:GetChildren())do if a:FindFirstChild("TouchInterest")then collectCoin(a)c=os.clock()break end end else goToLobby()end end
